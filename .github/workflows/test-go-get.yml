# File generated by gflows, do not modify
# Source: .gflows/workflows/test-go-get
name: build
"on":
  pull_request:
    branches:
    - develop
  push:
    branches:
    - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-go@v2
      with:
        go-version: ^1.14.4
    - run: go get github.com/jbrunton/gflows
    - run: gflows version
